using UnityEngine;

public class MainMusicSound : MonoBehaviour
{
    private static MainMusicSound instance;

    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}